
# Node.js + Express Backend (Day 3 KT Task)

## Steps to Run
1. Install Node.js
2. Run `npm install`
3. Run `npm start`
4. Server runs on http://localhost:3000

## API Endpoints
- GET /
- POST /users
- GET /users
- PUT /users/:id
- DELETE /users/:id
